# Open decisions

One line each: item · next action · who decides · since. Printed by
`.claude/hooks/orient.sh` every session. Remove a line when it is done; never
archive here. A `feedback` memory or "standing guidance" gets its line here
in the same session it is written. This list is the track — no new plan doc
for anything already on it.

- `tools/kanban_sync` plan-doc classification only runs on demand · run `/kanban-board-sync` at PR-open and plan-close (mechanical sources already run from `/checkpoint`), one writer at a time (`ListAgents` first) · me · 2026-08-28
- The fastapi container has no `git`, so `tests/test_quality_coordination_cleanup_actions.py`, `tests/test_cleanup_worktrees.py`, and the launcher-prelude tests skip or fail locally and only prove out in CI · add git to the image (`Dockerfile`) or keep trusting CI for them · you · 2026-08-28
- `advisory_engine` suggests on win rate alone, never cost/P&L · implement cost-aware suggestion or close · you (design approval) · 2026-08-22
- Banded cost-aware gate EV diagnostic (0.60–0.95 band negative-EV) · approve `docs/superpowers/specs/2026-08-26-economic-strategy-remediation-design.md` or close · you · 2026-08-26
- Shadow-mode evaluation stretch has never run · schedule a dated stretch · you · 2026-08-26
- Path-based CI test selection for code changes · find the original rejection incident, then decide · me → you · 2026-08-26
- RESOLVED 2026-08-30 (branch `fix/entry-gate-netting-remediation`): "`position_netting` closes lose money at scale" was traced UPSTREAM of the netting mechanism, which was working as designed — it correctly detects and closes positions that are already doomed, it does not create the loss. Root cause: `services/mutual_exclusivity.py`'s entry-side coverage gap. `find_me_pairs` only pairs an event when BOTH siblings appear in the same tick's watchlist-scoped `markets` list, so a fresh whale-signal candidate that was never on the watchlist got `me_complement=None` and the entry gate no-opped — the app bought YES on both sides of genuine 2-outcome markets at prices summing to 1.33 and 1.43 (`KXATPMATCH-26AUG28BUSBON`, both directions, within about an hour), a guaranteed loss by construction. Fixed by `find_open_confirmed_conflict`, which reads the persisted catalog-wide `market_titles`/`event_titles` caches instead, wired into `decision_bridge.py` as a fallback (bounded to the genuine 2-outcome case — N-way events stay out of scope, `position_netting` already manages that post-entry). Re-quantified at fix time: 36 `locked_loss` closes, -$3,610.49 total, of which $261.52 (7.2%) is exit-fee drag now tracked separately as `trades.netting_exit_fee_usd`; the other 92.8% is the entry-side loss this fix stops. The fee-vs-headroom tradeoff that $261.52 represents is NOT resolved here — it stays open on its own terms, see the `netting_exit_fee_usd` line below.
- `advisory_engine`'s reachable surface is narrow but wider than first stated (corrected 2026-08-30: the 11 hand-written paths, PLUS `strategy_overrides.by_category`/`by_series` and any of the 38 fingerprinted `strategy.*` fields via the variant branch); `whale_watcher_kalshi.min_contracts` rejections have been logged since it shipped and never once surfaced (the gap is named in the code's own 2026-08-14 comment) · wire `whale_watcher_kalshi`/`position_netting`/`risk` into advisory's config-path map, or scope this as intentionally strategy-only and close · you (design approval) · 2026-08-29
- `whale_confidence_weights` needs re-validation, not re-tuning: `depth_factor` shows the strongest negative correlation with correctness of any factor (r=-0.24, n=88,828, never checked before) despite already sitting at the config floor; `agreement_factor` now reads positive (r=+0.13), the opposite sign from the 2026-08-10 finding that its weight was set from; `confidence_scoring.py`'s docstring points to a settings.yaml comment that doesn't exist · re-run `whale_calibration`'s discrimination check against the current population and write the missing comment, or decide it's not worth revisiting · you (design approval) · 2026-08-29
- The longshot mechanism (`strategy.longshot_price_threshold`/`longshot_entry_threshold_bonus`/`longshot_close_window_sec`) is structurally dead: every price it targets (≤5¢ or ≥95¢) maps to unit_cost ≤0.05, which `min_unit_cost: 0.5` rejects unconditionally — 3 fields configured for a scenario that can never occur, not caught by the config-usage scanner since all three are read, just unreachable given a sibling gate · lower `min_unit_cost` to let it activate, remove the 3 dead fields, or confirm it's a deliberate no-op and close · you · 2026-08-29
- `position_netting.normal_volatility` (0.02) vs `strategy.auto_exit_normal_volatility` (0.002) baseline the same measure 10× apart. The zero-volatility filter defect in `_materiality_bar` is FIXED (PR #220, 2026-08-30) and was latent — it never fired on a measured close (10 of 11 recorded bars imply vol_ratio 0.574–2.29, inside the clamp) — so it is not the netting-loss explanation; `locked_loss` (26 of 37 rows) is the dominant path and never reads the bar · decide whether two baselines for one measure should differ 10×, or document why · you · 2026-08-29
- (1) DONE 2026-08-30 16:11 UTC by direct instruction: merged `origin/main` into the primary and restarted (`ddev restart`). Verified, not assumed: `/api/health/pipeline` 41-131s blocking → 0.375s; `python -m tools.soak_analyzer` first post-restart run PASSED 8 of 10 checks (all 6 data-layer checks clean: 0 drops, ticker conservation exact, staleness corroborated, queue headroom, resolver accounting) — the 2 FAILs (`capture_writer_health` 237 faults, `exit_engine_faults` 454 faults, both in the 24h fault-log window) are entirely pre-restart: checked every timestamp, 0 of either fault type occurred after 16:11:20 UTC. These will age out of the 24h window on their own; re-run the analyzer in ~24h to confirm no NEW faults accumulate before treating this as closed. **(2)** rewrite `docs/next-action.md`'s soak criteria against the corrected counters - superseded by (1)'s own analyzer output above; next-action.md updated directly instead of duplicating a criteria list here. **(3)** `realtime_data_plane.two_consumer_mode` permanence (P4 gate): `true` is live and the analyzer's first post-restart boundary passed; close the gate after 2-3 more clean boundaries (next check ~2026-08-31, then a few hours after) · run `python -m tools.soak_analyzer` at each boundary; decide permanent (update the file comment) or revert once you have 2-3 clean reads · you · 2026-08-30
- #214 self-feeding loop: trade analysis tunes on its own incomplete data with no provenance gate; `auto_apply_enabled` is off for both advisory and calibration, so the loop runs at human speed — but the human gate is uninformed (no recommendation shows the holes in its evidence). Design needed: provenance travels with every statistic; auto-apply refuses on degraded evidence. Same decision as the older line below about advisory suggesting on win rate alone, and as #51 (what governs re-arming auto-apply) — one design, not three · approve a design (brainstorming) or park explicitly · you · 2026-08-30
- RESOLVED 2026-08-30 by direct instruction: reversed the 2026-08-28 "never retire a tool" stance (`CLAUDE.md:120`) after `tools/kanban_sync`'s `find_by_marker` wrongly closed a real GitHub issue (#74) and `guard_workflow.py`'s R6 false-positive-blocked a read-only `git merge-tree` — both bugs fixed same night (issue #90 closed). New standard: prefer proven installed plugins/MCP servers/skills/commands over handspun equivalents; a handspun tool defaults to disabled until its own run history proves real value. AQC (`tools/quality_coordination.py`) is the first case under the new standard — see the line below.
- RESOLVED 2026-08-30: AQC's one real supervised `--clean` test (first ever, this session) confirmed the redundancy suspected above — 0 signals escalation-eligible, 0 cleanup actions taken, and `cleanup_actions` has 0 rows across all 11 runs in its history; every branch signal that ever escalated had already been deleted via the normal `gh pr merge --delete-branch`/`cleanup-worktrees.sh` path first. `_CLEANUP_ACTION_FOR_DOMAIN`'s `branch` → `delete_merged_branch` entry retired (empty dict now); `delete_merged_branch()` itself kept, tested, just no longer auto-selected. `ledger`/`process_hygiene` domains and `coordination_engine.py`'s state machine untouched — this finding is specific to the one cleanup action, not AQC as a whole.
- RESOLVED 2026-08-30 by direct instruction: `guard_workflow.py`'s R6 (peer-session git-merge guard) retired outright, not just its `merge-tree` regex bug — its liveness check had no time dimension, so a dead session's stale registry record counted identically to a live one, and it blocked a real merge over exactly that (a second, different zombie PID than the one in the friction log's item #3) the same night, after the regex fix already shipped. Zero known cases of R6 catching real concurrent-git corruption vs. two documented incidents of it blocking legitimate work. `_RISKY_GIT`/`other_sessions()` removed from `.claude/hooks/guard_workflow.py`; the session-registry/`--sessions` output it read stays, since `scripts/cleanup-worktrees.sh` and `orient.sh` still use it. `CLAUDE.md` and `.claude/rules/branching-and-ci.md` updated — "never checkout/stash/reset/rebase/merge under another session's work" is convention only now, not a harness-enforced guard.
- PR #202's event-scoped ME gate plan is NOT implementation-ready (5 of 6 spec-revision-2 defects still live in the task bodies; banner in the plan file). Separately: the gate BLOCKS second legs, i.e. it is a guard — and the 2026-08-30 instruction removed guards for training/analytics coverage. Decide whether this gate is wanted in paper mode at all before revising the plan · proceed to revision 3, or park until live-mode prep · you · 2026-08-30 — **narrowed 2026-08-30 by PR #298:** #298 fixes the identical root-cause diagnosis this spec's §1.2 already made (`find_me_pairs` blind to the watchlist-scoped entry stream) with a much lighter mechanism (a broad-cache read, no new `event_ticker` field on `Position`/`PendingOrder`, no limit-order-path plumbing) — but deliberately bounded to the 2-outcome case only, not §3.1's "uniform for 2-way and N-way" strict design. Once #298 merges, this plan's remaining live scope is: (a) whether to also block N-way ME events at entry (the still-open guard-vs-coverage question above, now scoped narrower), and (b) the richer capabilities #298 doesn't touch at all — `event_ticker` stamping as a first-class field, the limit-order/resting-order path (§4.5), and blocked-flip P&L measurement (§4.4). Not superseded outright — a peer session's initial read called it that; verified against the actual spec text before writing this line, since "same root cause" and "same scope" are different claims.
- Kalshi shipped Trade API 3.29.0 mid-audit: 154 of 227 mirrored pages had drifted (not the 5 first measured) · RESOLVED 2026-08-30 (PR #269): `get-balance`'s aggregation-semantics flip (now sums all exchange shards unless `exchange_index` is passed) was fixed by keeping the new aggregate meaning and relabeling the UI "(all shards)"; the account-diagnostics/historical-backfill/index-feed-backfill/MVE-discovery gaps the same re-sync surfaced were closed separately (PRs #272/#273/#274/#275, merged 2026-08-30)
- RESOLVED 2026-08-30 by direct instruction ("the app was never meant to be restricted to sports and crypto"): `kalshi.categories` widened live via `POST /api/config` (no restart — `ConfigStore.get()` re-reads the file independently of `uvicorn --reload`, which watches `.py` files only) from `[Sports]` to all 11 categories present in the app's own catalog: Sports, Crypto, Climate and Weather, Entertainment, Economics, Politics, Mentions, Commodities, Financials, Science and Technology, Elections. Immediate post-change read: `last_tick_duration_sec` 2.73s, 0 rate-limit hits, full read-token headroom — but `catalog_scan.py` batches 10 series/scan and deliberately yields to the tick-critical path, so the steady-state effect of ~11x more discoverable series (`len(categories) * top_n_per_category`, 12→132) won't be visible until it has cycled through the newly added categories — re-check `last_tick_duration_sec`/`catalog_scan` cadence/REST rate-limit headroom once it has, before calling this settled. Deeper finding from the same investigation: `strategy_engine.evaluate()`'s mutually-exclusive gate silently defaults to `False` (bare `except: pass`, no fault, no counter) whenever `event_titles` has no entry for a ticker — quantified 2026-08-30 at 180 distinct series / 14,180 of 95,535 signals, 97.6% of that one series family (`KXMVECROSSCATEGORY`, multivariate/combo events) which widening `categories` does NOT fix, since MVE markets aren't reachable via the regular series/category browsing at all (`get-multivariate-events`/`get-multivariate-event-collections` are separate, uncalled endpoints) · (1) verify the steady-state catalog-scan/tick-duration effect of the widened categories · (2) make the missing-`event_titles` path fail open-but-counted (mirrors PR #202's own spec pattern) so future gaps are visible instead of silent · **(3) DONE 2026-08-30, issue #268:** `services/market_watch/mve_scan.py` — `get_multivariate_event_collections`-driven series discovery (independent of `kalshi.categories`; root cause confirmed live: every MVE series reports `volume_fp: "0.00"` on its own `/series` entry, which `catalog_scan._get_series_cache`'s volume filter silently excludes regardless of category config) feeding `market_catalog.upsert_mve_markets` (`close_ts`-anchored, since MVE markets never carry `occurrence_datetime`) and `title_cache`'s `market_titles`/`event_titles` — see `docs/kalshi/CHEATSHEET.md`'s "How do you actually discover multivariate (combo) markets" entry for the full contract detail. (2) is a separate, still-open gap this feeds but does not close · me · 2026-08-30
- `position_netting`'s `locked_loss` branch closes immediately (frees bankroll/position headroom) rather than holding to Kalshi's fee-free settlement; the fee cost is now measured (`trades.netting_exit_fee_usd`) but the tradeoff itself is unresolved (**when reading that column: it is the GROUP TOTAL repeated on every member row of a multi-leg close, so `SUM(netting_exit_fee_usd)` overcounts by the group size — the correct aggregate is `SELECT SUM(fee) FROM trades WHERE netting_exit_fee_usd IS NOT NULL`, since `trades.fee` already holds each leg's own real per-leg fee**) · decide once a few weeks of the new column's data shows the real fee-vs-headroom tradeoff · you · 2026-08-30
- Score/clock coverage is still watchlist-scoped: `milestone_scan.py` (2026-08-30) builds a broad `event_ticker -> milestone_id` cache, but `live_status._fetch_live_status` still picks WHICH events to poll entirely from its per-tick `markets` argument and only consults that cache for events already picked (and already truncated to `_LIVE_STATUS_MAX_POLL_PER_TICK`), so `game_state` sees exactly the event set it saw before — that branch only removed redundant per-event `get_milestones_for_event` REST calls. Real broadening (polling milestone-known events that never appear in `markets`) is unimplemented and changes what the per-tick poll budget is spent on · design and review it on its own terms, or decide the watchlist scope is what's wanted for score/clock · you (design approval) · 2026-08-30
- PR #307 broadened CLAUDE.md's dimensional-analysis HARD RULE to any arithmetic/unit conversion, not just money/probability math, but `.claude/hooks/guard_workflow.py`'s automated nudge (`post()`, `HOT_PATHS`/`_is_money_ui`) still only fires on the old narrower money/probability-shaped scope — flagged by both the PR's self-review and its independent adversarial review · broaden the hook's detection to match the rule, or decide the wider scope stays session-enforced-only and close · you (design approval) · 2026-08-31
- Exhaustive claudesuperpower.com toolkit scan (filed `docs/superpowers/research/2026-08-31-claudesuperpower-toolkit-assessment.md`, user-confirmed 2026-08-31) recommends piloting 4 official `claude-plugins-official` plugins in priority order — `pr-review-toolkit` (silent-failure/PR-test-coverage subagents matching two things CLAUDE.md already mandates by hand), `claude-security` (deep vuln scan, audit-only per this repo's automation restrictions), `claude-md-management` (audit-only pass, its "quality template" suggestions need a sanity check against this repo's deliberate terseness convention before accepting), `codspeed` (hot-path benchmarking for the data-plane HARD RULE's "measured before it ships" requirement) — plus explicit non-adoptions (every Kalshi/Polymarket-named third-party MCP, for credential-custody and Kalshi-integration-authority conflicts) and one item blocked on the "no deployment target" open item (`sentry`/`grafana`). Nothing installed yet — installing any of these changes `.claude/settings.json` and, for the security/CLAUDE.md-editing ones, grants write access to CLAUDE.md itself, which the assessment itself flags as needing explicit go-ahead rather than a unilateral install · run a `superpowers:brainstorming`/`writing-plans` sequence off this research doc to decide which (if any) to pilot, or close · you (design approval) · 2026-08-31
