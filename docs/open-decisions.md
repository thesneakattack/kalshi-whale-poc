# Open decisions

One line each: item · next action · who decides · since. Printed by
`.claude/hooks/orient.sh` every session. Remove a line when it is done; never
archive here. A `feedback` memory or "standing guidance" gets its line here
in the same session it is written. This list is the track — no new plan doc
for anything already on it.

- `tools/kanban_sync` plan-doc classification only runs on demand · run `/kanban-board-sync` at PR-open and plan-close (mechanical sources already run from `/checkpoint`), one writer at a time (`ListAgents` first) · me · 2026-08-28
- `advisory_engine` suggests on win rate alone, never cost/P&L · implement cost-aware suggestion or close · you (design approval) · 2026-08-22
- Banded cost-aware gate EV diagnostic (0.60–0.95 band negative-EV) · approve `docs/superpowers/specs/2026-08-26-economic-strategy-remediation-design.md` or close · you · 2026-08-26
- Shadow-mode evaluation stretch has never run · schedule a dated stretch · you · 2026-08-26
- Path-based CI test selection for code changes · find the original rejection incident, then decide · me → you · 2026-08-26
- `advisory_engine`'s reachable surface is narrow but wider than first stated (corrected 2026-08-30: the 11 hand-written paths, PLUS `strategy_overrides.by_category`/`by_series` and any of the 38 fingerprinted `strategy.*` fields via the variant branch); `whale_watcher_kalshi.min_contracts` rejections have been logged since it shipped and never once surfaced (the gap is named in the code's own 2026-08-14 comment) · wire `whale_watcher_kalshi`/`position_netting`/`risk` into advisory's config-path map, or scope this as intentionally strategy-only and close · you (design approval) · 2026-08-29
- `whale_confidence_weights` needs re-validation, not re-tuning: `depth_factor` shows the strongest negative correlation with correctness of any factor (r=-0.24, n=88,828, never checked before) despite already sitting at the config floor; `agreement_factor` now reads positive (r=+0.13), the opposite sign from the 2026-08-10 finding that its weight was set from; `confidence_scoring.py`'s docstring points to a settings.yaml comment that doesn't exist · re-run `whale_calibration`'s discrimination check against the current population and write the missing comment, or decide it's not worth revisiting · you (design approval) · 2026-08-29
- The longshot mechanism (`strategy.longshot_price_threshold`/`longshot_entry_threshold_bonus`/`longshot_close_window_sec`) is structurally dead: every price it targets (≤5¢ or ≥95¢) maps to unit_cost ≤0.05, which `min_unit_cost: 0.5` rejects unconditionally — 3 fields configured for a scenario that can never occur, not caught by the config-usage scanner since all three are read, just unreachable given a sibling gate · lower `min_unit_cost` to let it activate, remove the 3 dead fields, or confirm it's a deliberate no-op and close · you · 2026-08-29
- `position_netting.normal_volatility` (0.02) vs `strategy.auto_exit_normal_volatility` (0.002) baseline the same measure 10× apart. The zero-volatility filter defect in `_materiality_bar` is FIXED (PR #220, 2026-08-30) and was latent — it never fired on a measured close (10 of 11 recorded bars imply vol_ratio 0.574–2.29, inside the clamp) — so it is not the netting-loss explanation; `locked_loss` (26 of 37 rows) is the dominant path and never reads the bar · decide whether two baselines for one measure should differ 10×, or document why · you · 2026-08-29
- `realtime_data_plane.two_consumer_mode` permanence (P4 gate): re-run 2026-09-01 (~14h past the 2026-08-31 16:11 UTC restart boundary) still VERDICT FAIL — `capture_writer_health` (87 faults) and `exit_engine_faults` (314) verified live as actively recurring, not decaying pre-restart tail (`capture_writer`'s `flush_retained_on_lock` last occurred 06:06:23Z, `exit_engine`'s `stale_price_uncorroborated` at 06:07:09Z, both minutes before the check). Do not close this gate on a clean-looking read without checking fault recency the same way. `capture_writer`'s fault is tracked (issue #211, likely the same lock-contention path); `exit_engine`'s `stale_price_uncorroborated` has no tracked issue yet · root-cause both via systematic-debugging before any further boundary check is treated as informative; file an issue for the exit_engine fault · you · 2026-09-01
- PR #202's event-scoped ME gate plan is retired — PR #298 already shipped a working fix for the 2-outcome case (`find_open_confirmed_conflict`). Real scope remains open beyond that: N-way (3+-outcome) ME events, `event_ticker` as a first-class field on `Position`/`PendingOrder`, the limit-order/resting-order path, blocked-flip P&L measurement — preserved as GitHub issues #277/#289–293, not lost with the plan's retirement · a future N-way design should start from PR #298's actual shipped code · you (design approval, whenever picked back up) · 2026-08-31
- `kalshi.categories` widened to all 11 categories (2026-08-30, direct instruction: "never meant to be restricted to sports and crypto"). Two follow-ups still open: (1) verify the steady-state `catalog_scan` cadence/REST rate-limit effect of ~11x more discoverable series — never checked after the initial post-change read; (2) `strategy_engine.evaluate()`'s mutually-exclusive gate silently defaults to `False` (bare `except: pass`, no fault, no counter) whenever `event_titles` has no entry for a ticker — quantified at 14,180 of 95,535 signals, 97.6% one series family (`KXMVECROSSCATEGORY`, MVE/combo events, unreachable via regular category browsing) — make it fail open-but-counted, mirroring PR #202's own spec pattern, or accept the silence. (MVE series discovery itself is done, issue #268.) · you (1) / me (2, design approval) · 2026-08-30
- `position_netting`'s `locked_loss` branch closes immediately (frees bankroll/position headroom) rather than holding to Kalshi's fee-free settlement; the fee cost is now measured (`trades.netting_exit_fee_usd`) but the tradeoff itself is unresolved (**when reading that column: it is the GROUP TOTAL repeated on every member row of a multi-leg close, so `SUM(netting_exit_fee_usd)` overcounts by the group size — the correct aggregate is `SELECT SUM(fee) FROM trades WHERE netting_exit_fee_usd IS NOT NULL`, since `trades.fee` already holds each leg's own real per-leg fee**) · decide once a few weeks of the new column's data shows the real fee-vs-headroom tradeoff · you · 2026-08-30
- Score/clock coverage is still watchlist-scoped: `milestone_scan.py` (2026-08-30) builds a broad `event_ticker -> milestone_id` cache, but `live_status._fetch_live_status` still picks WHICH events to poll entirely from its per-tick `markets` argument, so `game_state` sees exactly the event set it saw before. Real broadening (polling milestone-known events that never appear in `markets`) is unimplemented and changes what the per-tick poll budget is spent on · design and review it on its own terms, or decide the watchlist scope is what's wanted for score/clock · you (design approval) · 2026-08-30
- PR #307 broadened CLAUDE.md's dimensional-analysis HARD RULE to any arithmetic/unit conversion, not just money/probability math, but `.claude/hooks/guard_workflow.py`'s automated nudge (`post()`, `HOT_PATHS`/`_is_money_ui`) still only fires on the old narrower money/probability-shaped scope · broaden the hook's detection to match the rule, or decide the wider scope stays session-enforced-only and close · you (design approval) · 2026-08-31
- Exhaustive claudesuperpower.com toolkit scan recommends piloting 4 official `claude-plugins-official` plugins in priority order — `pr-review-toolkit`, `claude-security`, `claude-md-management`, `codspeed` — plus explicit non-adoptions and one item blocked on "no deployment target" (`sentry`/`grafana`). Full research→design→plan pipeline complete, each stage through its own review cycle. Two corrections worth knowing: `claude-security` does NOT grant CLAUDE.md write access (only `claude-md-management` does); `codspeed` requires an external CodSpeed account (`codspeed auth login`), its own separate go-ahead. Nothing installed yet · give the plan's Task 1 go-ahead (plugins 1–3), and separately decide Task 5 (codspeed's external account), or decline and close · you (design approval) · 2026-08-31
- Weather index ingestion (`docs/superpowers/specs/2026-08-30-weather-index-ingestion-design.md`, PR #263) — design-then-plan pipeline complete, each stage through its own review cycle. Ingestion only: no settlement-edge validation study, no entry logic, nothing wired into any strategy. Nothing built yet · give the plan's go-ahead to start Task 1, or decline and close · you (design approval) · 2026-08-31
- kalshi-category-data-completeness Task 12 seeded `config/settings.yaml`'s Pyth Commodities feed with 2 real underlyings (gold, silver) rather than `underlying_tickers: ["all"]`, which `docs/kalshi/pyth-value.md:27` documents as a real option — the narrower choice was never an explicit go/no-go, it just matched the plan's own example values · confirm the 2-underlying scope is intentional, or widen to `["all"]` (config-only change) · you · 2026-08-31
- `propagate_milestone_winners`'s (`services/market_watch/catalog_scan.py`) `related_market_by_ticker` lookup feeds documented EVENT tickers into `get_markets_by_tickers`, whose filter is documented as MARKET tickers — live-verified: 710 real `related_event_tickers` → 0 markets returned. Pre-existing (predates this session's work), sits underneath structured-target resolution and `candidate_id_mapping` widening, which cannot execute against real winners in production until it's fixed · decide priority and approach (resolve each `related_event_tickers` entry to its own child markets first), or accept this path stays non-functional for structured/political-race markets for now · you (design approval) · 2026-08-31
- whale-confidence-scoring-remediation (PR #388) fixed tie-blind bucketing, correctly reclassifying 5 of 9 factors as `data_status: "contaminated"` — but `_suggested_weights` has no `data_status` awareness and no minimum-survivor guard, so `suggested_weights` now collapses to 2 factors (`context_factor 0.95, depth_factor 0.05`) and would move the blended weight `0.3434 → 0.5437` if applied. `auto_apply_enabled` stays `false` (nothing writes this automatically), but the dashboard's manual "Apply suggested weights" button has no guard against writing this concentrated blend — Task 12's `measurement_is_valid` gate is the named fix, blocked behind Task 10's soak-time re-measurement gate · do not click "Apply suggested weights" until Task 12 ships · you (avoid clicking) / me (land Task 12 once Task 10 clears) · 2026-09-01
- Same PR's Task 1 tie-detection (`_tied_run_size`) has 2 known precision gaps, found with concrete falsifying data, not fixed: (1) flags contamination on the FULL tied-run length at a cut rather than just the minority side crossing the boundary — can discard a real measurement over ~1% boundary arbitrariness; (2) `max(30, 0.005*n)`'s absolute+fractional floor mix creates a blind band below n≈6000 where arrival order alone can flip the verdict — not reachable at today's n=103k+, but latent if `min_resolved_signals` is ever lowered · consider before Task 10's re-measurement gate treats today's algorithm as final · you (design call — a real algorithm change) · 2026-09-01
- `yes_ask_dollars`'s value when a market genuinely has no resting ask is unconfirmed against `docs/kalshi/` — `sample_event_response.json:158` shows `"1.0000"` for what may be exactly that case; if so, `raw_spread`/`input_coverage`'s absence-rate understates how often spread is genuinely unmeasurable · verify against a live no-ask market or current official Kalshi docs before reading spread coverage numbers as ground truth · you or me · 2026-09-01
- PR #394 surfaced 2 non-blocking findings, not fixed: (1) a real buffer-race regression test doesn't reliably reproduce under CPython's default GIL scheduling despite the fix being sound (forced-interleaving repro confirmed it); (2) `config_performance.py`'s `record_variant()` has the same unawaited-sync-write shape the PR fixed elsewhere (lower risk: idempotent `INSERT OR IGNORE`) · (1) strengthen with explicit thread-coordination or accept the weaker guard; (2) route through `tick_executor` too, or confirm low risk and close · you (design call, low priority) · 2026-09-01
- `tools/soak_analyzer.py` only gates on `capture_writer` faults; 3 sibling stores (`game_state`/`series_watcher`/`settlement_edge`) now fault-log for parity but have no automated soak check, only manual `/api/health/faults` review · add a parallel/broadened check, or decide manual review suffices for these lower-volume stores · you (design approval) · 2026-09-01
- `check_event_loop_stalls`/`check_exit_engine_faults` can never report `PASS` on a genuinely healthy window — `fault_log.summary()`'s zero-fault components are *absent* from the dict, not present with `0`, so both checks read "absent" as `UNKNOWN` and drag the whole verdict to `UNKNOWN`. Separately, stall severity is keyed on peak magnitude only, never `stall_count` — many sub-1000ms stalls every window always logs `"warn"` · design an "queried-zero" vs "field-absent" distinction, or accept `UNKNOWN`-on-healthy; decide whether severity should also key on frequency · you (design approval) · 2026-09-01
- `check_series_funnel` computes `reconcile()`/`funnel()` separately per call, each independently re-querying the same data — x2 redundant queries × 8 watched series per `run_offline()` call. Pre-existing, more relevant now that it's serialized onto one shared `_aio_db` connection per file rather than spread across old per-call connections · measure the real cost, then thread one computation through both uses or decide the duplication is acceptable · you (design call) · 2026-09-01
- 15 `docs/superpowers/plans/*.md` past the 72h ledger floor (dated 2026-08-24 through 2026-08-28, none newer) and 2 branches past the 8h floor (`feat/candlestick-volatility`, `feat/frontend-realtime-push` — pushed, no PR ever opened) · triage: open/merge/close a PR or delete the 2 branches; confirm via `tools/kanban_sync`'s classification whether each stale plan actually shipped or should be formally retired · you · 2026-09-02
- Backfill the 2026-09-02 08:24–14:45 UTC `raw_trades` hole (fd-exhaustion incident, ~1,127 capture batches dropped) from Kalshi's REST trade history, or accept it? `raw_trades` is never pruned by design; REST cost is bounded by the watchlist and window · you (cost/value) · 2026-09-02
- `market_history.db`'s 2026-09-03 corruption was fixed live (SQLite `.recover`, ~15h fresher than the only backup; `outcomes` table never corrupted, `snapshots` lost ~2.5% of rows; original preserved at `data/quarantine/20260903T034119Z-market_history-corrupt/`). Root cause still unconfirmed — a timing correlation with that worker's fd-pressure window is a specific correlation, not a proven mechanism · watch for recurrence once PR #441's connection-leak fix actually lands (the fix is still plan-only, not implemented — see below) · you or me · 2026-09-03
- PR #441 (merged) is a **plan document only** — none of its 10 code tasks are implemented, and it doesn't cover the second-pass audit's own Tier 0 items 3 (`/api/health/faults`'s `hours` scoping bug), 4 (raw_trades backfill, separate line above), 5 (fault_log alerting consumer), 6 (observability monitor surviving its own stalls). Tier 1 (7 items), Tier 2 (10, including the persistence-module rewrite and DuckDB benchmark), Tier 3 (4) are completely unplanned · pick up PR #441's own Tasks 1-7/9/10 first, then decide whether items 3/5/6 fold into a follow-up plan or their own; Tier 1-3 need their own planning pass when prioritized · you (priority call) · 2026-09-03
- `config/settings.yaml` has an uncommitted working-tree change (re-check with `git diff` — this predates tonight's separate `auto_exit_enabled` pause and may have changed again): `kalshi.markets_watchlist_mode: merge → exclusive`, `kalshi.max_children_per_parent: 5 → 0`, `kalshi.categories` narrowed to two, and the calibration-audit comment block wiped a third time by the Controls panel's whole-section Save (a real bug: `config-panel.js` resends each section as a new object, `config_store.update()` shallow-merges over the ruamel node the comment was attached to). All four need one decision together — restore the comment only after the merge bug is fixed or it will be wiped again · you · 2026-09-03
- Persistence layer, now that "one SQLite file per concern" is gone: build one shared `connect()`/pragma-policy/DDL-registry module — hand-rolled or on SQLAlchemy Core's pool? Benchmark both on the fd census and the 548 µs connect cost; consolidating the ~14 sub-1 MB files into one is a separate question the same benchmark answers · you (design call) · 2026-09-02
- DuckDB for `raw_trades`: the strongest contention-based case was a misread fault count and is withdrawn; the remaining case is an unmeasured scan/compression estimate on a 27–29 GB file · benchmark DuckDB and Parquet+DuckDB against the queries that read `raw_trades` today, then go/no-go · you (go/no-go) · 2026-09-02
- Alerting coverage: `check_and_alert` monitors kill switch + stream connectivity only, never reads `fault_log` — a day with 1,042 error-severity faults produced zero alerts · make error-fault bursts an alert category, or schedule `soak_analyzer`'s checks in-process as the consumer · you (design) · 2026-09-02
- Process rule amendment: a finding that first appears in an adversarial review is a new claim and meets the artifact's own evidence standard before consolidation adopts it (a wrong finding once entered a report at exactly that step) — a CLAUDE.md change with its own review cycle · accept or decline · you · 2026-09-02
- Where does config documentation live once `config/settings.yaml` is values-only (a pydantic schema's field descriptions, `config/README.md`, or research docs)? · you · 2026-09-02
- Edge gate's P_pre-unavailable fail-open default (`strategy_engine.py`'s `_edge_gate_check`) is shipped behavior, not yet revisited against real data · revisit once live validation/markout data exists · you · 2026-09-03
- GitHub's branch-protection API (`repos/.../branches/main/protection`) returns 403 on this repo — a private-repo plan limitation. `mergeStateStatus: CLEAN` cannot be cross-checked against the actual required-contexts list; confirmed the same night that a CI outage can leave `mergeStateStatus: CLEAN` with zero checks having run. The per-commit status API (`.../commits/<sha>/status`) is unaffected and remains the reliable read · confirm whether a plan upgrade restores the endpoint, or accept the commit-status-API + documented-list combination as standing practice · you · 2026-09-05
- `ef662c0` (`kelly_fraction_of_cap` + `KXBTC15M`) sits unpushed on the primary's local `main` · needs a home — decide when/whether to push it · you · 2026-09-05
- The "scrap derived datasets, rebuild on `signal_log`" plan (your stated intent, 2026-09-04) was made while the pipeline was still broken. `#577` (fabricated-price fix) and `#581` (tick_executor contention fix) are both merged and live now · worth a fresh look at whether the rebuild is still wanted, rather than assuming the original plan still applies unchanged · you · 2026-09-05
- `#532` (`rejection_events` unbounded growth, 29.8M rows, 98.98% from the `min_contracts` gate alone, which has ~23.8M resolved samples against a `min_samples=30` threshold) — recommendation: sample `min_contracts` at write time, keep every other gate whole, since a blanket retention window would starve the fragile gates that are only ~1% of volume · go/no-go on the sampling design, or a different retention call · you · 2026-09-05
- RESOLVED 2026-09-05: `#578` purged clean (David confirmed the destructive step directly; issue closed) — 5,049,063 pre-fix `market_history.snapshots` rows deleted via the existing, production-tested `prune()`, cutoff pinned to `#577`'s fix commit (`da4b93a`, 2026-09-05T10:16:11 UTC). Verified before/after: 0 pre-fix rows remain, 0 hard-lower-bound fabricated rows, `PRAGMA integrity_check: ok`, backup retained at `data/backups_large/20260905T174550Z/`. `#589` (the `price_source` provenance column this purge was gating) resolves to its own option 1 by construction: moot, since the table these figures characterized no longer exists — a future need for provenance tracking is a fresh design question against whatever schema is live then, not a continuation of this one.
