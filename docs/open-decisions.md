# Open decisions

One line each: item · next action · who decides · since. Printed by
`.claude/hooks/orient.sh` every session. Remove a line when it is done; never
archive here. A `feedback` memory or "standing guidance" gets its line here
in the same session it is written. This list is the track — no new plan doc
for anything already on it.

- `tools/kanban_sync` plan-doc classification only runs on demand · run `/kanban-board-sync` at PR-open and plan-close (mechanical sources already run from `/checkpoint`), one writer at a time (`ListAgents` first) · me · 2026-08-28
- The fastapi container has no `git`, so `tests/test_quality_coordination_cleanup_actions.py`, `tests/test_cleanup_worktrees.py`, and the launcher-prelude tests skip or fail locally and only prove out in CI · add git to the image (`Dockerfile`) or keep trusting CI for them · you · 2026-08-28
- `advisory_engine` suggests on win rate alone, never cost/P&L · implement cost-aware suggestion or close · you (design approval) · 2026-08-22
- Banded cost-aware gate EV diagnostic (0.60–0.95 band negative-EV) · approve `docs/superpowers/specs/2026-08-26-economic-strategy-remediation-design.md` or close · you · 2026-08-26
- Shadow-mode evaluation stretch has never run · schedule a dated stretch · you · 2026-08-26
- Path-based CI test selection for code changes · find the original rejection incident, then decide · me → you · 2026-08-26
- RESOLVED 2026-08-30 (branch `fix/entry-gate-netting-remediation`): "`position_netting` closes lose money at scale" was traced UPSTREAM of the netting mechanism, which was working as designed — it correctly detects and closes positions that are already doomed, it does not create the loss. Root cause: `services/mutual_exclusivity.py`'s entry-side coverage gap. `find_me_pairs` only pairs an event when BOTH siblings appear in the same tick's watchlist-scoped `markets` list, so a fresh whale-signal candidate that was never on the watchlist got `me_complement=None` and the entry gate no-opped — the app bought YES on both sides of genuine 2-outcome markets at prices summing to 1.33 and 1.43 (`KXATPMATCH-26AUG28BUSBON`, both directions, within about an hour), a guaranteed loss by construction. Fixed by `find_open_confirmed_conflict`, which reads the persisted catalog-wide `market_titles`/`event_titles` caches instead, wired into `decision_bridge.py` as a fallback (bounded to the genuine 2-outcome case — N-way events stay out of scope, `position_netting` already manages that post-entry). Re-quantified at fix time: 36 `locked_loss` closes, -$3,610.49 total, of which $261.52 (7.2%) is exit-fee drag now tracked separately as `trades.netting_exit_fee_usd`; the other 92.8% is the entry-side loss this fix stops. The fee-vs-headroom tradeoff that $261.52 represents is NOT resolved here — it stays open on its own terms, see the `netting_exit_fee_usd` line below.
- `advisory_engine`'s reachable surface is narrow but wider than first stated (corrected 2026-08-30: the 11 hand-written paths, PLUS `strategy_overrides.by_category`/`by_series` and any of the 38 fingerprinted `strategy.*` fields via the variant branch); `whale_watcher_kalshi.min_contracts` rejections have been logged since it shipped and never once surfaced (the gap is named in the code's own 2026-08-14 comment) · wire `whale_watcher_kalshi`/`position_netting`/`risk` into advisory's config-path map, or scope this as intentionally strategy-only and close · you (design approval) · 2026-08-29
- `whale_confidence_weights` needs re-validation, not re-tuning: `depth_factor` shows the strongest negative correlation with correctness of any factor (r=-0.24, n=88,828, never checked before) despite already sitting at the config floor; `agreement_factor` now reads positive (r=+0.13), the opposite sign from the 2026-08-10 finding that its weight was set from; `confidence_scoring.py`'s docstring points to a settings.yaml comment that doesn't exist · re-run `whale_calibration`'s discrimination check against the current population and write the missing comment, or decide it's not worth revisiting · you (design approval) · 2026-08-29
- The longshot mechanism (`strategy.longshot_price_threshold`/`longshot_entry_threshold_bonus`/`longshot_close_window_sec`) is structurally dead: every price it targets (≤5¢ or ≥95¢) maps to unit_cost ≤0.05, which `min_unit_cost: 0.5` rejects unconditionally — 3 fields configured for a scenario that can never occur, not caught by the config-usage scanner since all three are read, just unreachable given a sibling gate · lower `min_unit_cost` to let it activate, remove the 3 dead fields, or confirm it's a deliberate no-op and close · you · 2026-08-29
- `position_netting.normal_volatility` (0.02) vs `strategy.auto_exit_normal_volatility` (0.002) baseline the same measure 10× apart. The zero-volatility filter defect in `_materiality_bar` is FIXED (PR #220, 2026-08-30) and was latent — it never fired on a measured close (10 of 11 recorded bars imply vol_ratio 0.574–2.29, inside the clamp) — so it is not the netting-loss explanation; `locked_loss` (26 of 37 rows) is the dominant path and never reads the bar · decide whether two baselines for one measure should differ 10×, or document why · you · 2026-08-29
- (1) DONE 2026-08-30 16:11 UTC by direct instruction: merged `origin/main` into the primary and restarted (`ddev restart`). Verified, not assumed: `/api/health/pipeline` 41-131s blocking → 0.375s; `python -m tools.soak_analyzer` first post-restart run PASSED 8 of 10 checks (all 6 data-layer checks clean: 0 drops, ticker conservation exact, staleness corroborated, queue headroom, resolver accounting) — the 2 FAILs (`capture_writer_health` 237 faults, `exit_engine_faults` 454 faults, both in the 24h fault-log window) are entirely pre-restart: checked every timestamp, 0 of either fault type occurred after 16:11:20 UTC. These will age out of the 24h window on their own; re-run the analyzer in ~24h to confirm no NEW faults accumulate before treating this as closed. **(2)** rewrite `docs/next-action.md`'s soak criteria against the corrected counters - superseded by (1)'s own analyzer output above; next-action.md updated directly instead of duplicating a criteria list here. **(3)** `realtime_data_plane.two_consumer_mode` permanence (P4 gate): `true` is live and the analyzer's first post-restart boundary passed; close the gate after 2-3 more clean boundaries (next check ~2026-08-31, then a few hours after) · run `python -m tools.soak_analyzer` at each boundary; decide permanent (update the file comment) or revert once you have 2-3 clean reads · you · 2026-08-30
- RESOLVED 2026-08-31 (PR #297, merged 2026-08-30T23:21Z): #214 self-feeding loop's provenance gate is shipped, not just designed. `services/quality/evidence_provenance.py`'s `current_completeness_state()` composes three already-public defect counters (`settlement_resolver.dropped_after_max_attempts`, `index_feed.dropped_rows`, `capture_writer.dropped_count`/`overflow_dropped_count`) into one "is a known data-completeness defect active right now" read; verified live on `origin/main` (`main.py:126,484,576`) — both config-write auto-apply paths (advisory + calibration) now refuse to write while `degraded` is true. Design spec: `docs/superpowers/specs/2026-08-30-self-feeding-loop-provenance-design.md`; plan: `docs/superpowers/plans/2026-08-30-self-feeding-loop-provenance.md`. Known limitation (documented, not fixed): all four counters are lifetime/monotonic, so `degraded` latches `True` for the rest of a process's uptime after one occurrence, even once the underlying defect is fixed — a future delta-based comparison is the named eventual fix. Out of scope, still open on their own lines: the `advisory_engine suggests on win rate alone, never cost/P&L` line above and #51 (re-arming either `auto_apply_enabled` flag — still a parked human decision; this PR only makes the loop safe to run at whatever speed it's later un-paused to).
- RESOLVED 2026-08-30 by direct instruction: reversed the 2026-08-28 "never retire a tool" stance (`CLAUDE.md:120`) after `tools/kanban_sync`'s `find_by_marker` wrongly closed a real GitHub issue (#74) and `guard_workflow.py`'s R6 false-positive-blocked a read-only `git merge-tree` — both bugs fixed same night (issue #90 closed). New standard: prefer proven installed plugins/MCP servers/skills/commands over handspun equivalents; a handspun tool defaults to disabled until its own run history proves real value. AQC (`tools/quality_coordination.py`) is the first case under the new standard — see the line below.
- RESOLVED 2026-08-30: AQC's one real supervised `--clean` test (first ever, this session) confirmed the redundancy suspected above — 0 signals escalation-eligible, 0 cleanup actions taken, and `cleanup_actions` has 0 rows across all 11 runs in its history; every branch signal that ever escalated had already been deleted via the normal `gh pr merge --delete-branch`/`cleanup-worktrees.sh` path first. `_CLEANUP_ACTION_FOR_DOMAIN`'s `branch` → `delete_merged_branch` entry retired (empty dict now); `delete_merged_branch()` itself kept, tested, just no longer auto-selected. `ledger`/`process_hygiene` domains and `coordination_engine.py`'s state machine untouched — this finding is specific to the one cleanup action, not AQC as a whole.
- RESOLVED 2026-08-30 by direct instruction: `guard_workflow.py`'s R6 (peer-session git-merge guard) retired outright, not just its `merge-tree` regex bug — its liveness check had no time dimension, so a dead session's stale registry record counted identically to a live one, and it blocked a real merge over exactly that (a second, different zombie PID than the one in the friction log's item #3) the same night, after the regex fix already shipped. Zero known cases of R6 catching real concurrent-git corruption vs. two documented incidents of it blocking legitimate work. `_RISKY_GIT`/`other_sessions()` removed from `.claude/hooks/guard_workflow.py`; the session-registry/`--sessions` output it read stays, since `scripts/cleanup-worktrees.sh` and `orient.sh` still use it. `CLAUDE.md` and `.claude/rules/branching-and-ci.md` updated — "never checkout/stash/reset/rebase/merge under another session's work" is convention only now, not a harness-enforced guard.
- **RESOLVED 2026-08-31 — retired, not revised:** PR #202's event-scoped ME gate plan (`docs/superpowers/plans/2026-08-29-event-scoped-me-gate.md`) is retired outright. Re-verified against current source: its Task 3 instructs removing/replacing the `me_complement` parameter, but PR #298 (merged) already *extended* that exact parameter with a shipped, working fix (`find_open_confirmed_conflict` in `services/mutual_exclusivity.py`, wired into `decision_bridge.py:117-120`) — executing Task 3 today would regress shipped code, not close a gap, so "proceed to revision 3" (the prior next-action on this line) was the wrong call once that was found; retirement is. PR #298's fix is deliberately bounded to the 2-outcome case only (not §3.1's N-way design), so real scope remains open: N-way (3+-outcome) ME events, `event_ticker` as a first-class field on `Position`/`PendingOrder` (§4.1), the limit-order/resting-order path (§4.5), blocked-flip P&L measurement (§4.4). Preserved as GitHub issues #277/#289–293 in the generic "Unplanned" milestone (#11), not lost with the plan's retirement · a future N-way design should start from PR #298's actual shipped code · you (design approval, whenever picked back up) · 2026-08-31
- Kalshi shipped Trade API 3.29.0 mid-audit: 154 of 227 mirrored pages had drifted (not the 5 first measured) · RESOLVED 2026-08-30 (PR #269): `get-balance`'s aggregation-semantics flip (now sums all exchange shards unless `exchange_index` is passed) was fixed by keeping the new aggregate meaning and relabeling the UI "(all shards)"; the account-diagnostics/historical-backfill/index-feed-backfill/MVE-discovery gaps the same re-sync surfaced were closed separately (PRs #272/#273/#274/#275, merged 2026-08-30)
- RESOLVED 2026-08-30 by direct instruction ("the app was never meant to be restricted to sports and crypto"): `kalshi.categories` widened live via `POST /api/config` (no restart — `ConfigStore.get()` re-reads the file independently of `uvicorn --reload`, which watches `.py` files only) from `[Sports]` to all 11 categories present in the app's own catalog: Sports, Crypto, Climate and Weather, Entertainment, Economics, Politics, Mentions, Commodities, Financials, Science and Technology, Elections. Immediate post-change read: `last_tick_duration_sec` 2.73s, 0 rate-limit hits, full read-token headroom — but `catalog_scan.py` batches 10 series/scan and deliberately yields to the tick-critical path, so the steady-state effect of ~11x more discoverable series (`len(categories) * top_n_per_category`, 12→132) won't be visible until it has cycled through the newly added categories — re-check `last_tick_duration_sec`/`catalog_scan` cadence/REST rate-limit headroom once it has, before calling this settled. Deeper finding from the same investigation: `strategy_engine.evaluate()`'s mutually-exclusive gate silently defaults to `False` (bare `except: pass`, no fault, no counter) whenever `event_titles` has no entry for a ticker — quantified 2026-08-30 at 180 distinct series / 14,180 of 95,535 signals, 97.6% of that one series family (`KXMVECROSSCATEGORY`, multivariate/combo events) which widening `categories` does NOT fix, since MVE markets aren't reachable via the regular series/category browsing at all (`get-multivariate-events`/`get-multivariate-event-collections` are separate, uncalled endpoints) · (1) verify the steady-state catalog-scan/tick-duration effect of the widened categories · (2) make the missing-`event_titles` path fail open-but-counted (mirrors PR #202's own spec pattern) so future gaps are visible instead of silent · **(3) DONE 2026-08-30, issue #268:** `services/market_watch/mve_scan.py` — `get_multivariate_event_collections`-driven series discovery (independent of `kalshi.categories`; root cause confirmed live: every MVE series reports `volume_fp: "0.00"` on its own `/series` entry, which `catalog_scan._get_series_cache`'s volume filter silently excludes regardless of category config) feeding `market_catalog.upsert_mve_markets` (`close_ts`-anchored, since MVE markets never carry `occurrence_datetime`) and `title_cache`'s `market_titles`/`event_titles` — see `docs/kalshi/CHEATSHEET.md`'s "How do you actually discover multivariate (combo) markets" entry for the full contract detail. (2) is a separate, still-open gap this feeds but does not close · me · 2026-08-30
- `position_netting`'s `locked_loss` branch closes immediately (frees bankroll/position headroom) rather than holding to Kalshi's fee-free settlement; the fee cost is now measured (`trades.netting_exit_fee_usd`) but the tradeoff itself is unresolved (**when reading that column: it is the GROUP TOTAL repeated on every member row of a multi-leg close, so `SUM(netting_exit_fee_usd)` overcounts by the group size — the correct aggregate is `SELECT SUM(fee) FROM trades WHERE netting_exit_fee_usd IS NOT NULL`, since `trades.fee` already holds each leg's own real per-leg fee**) · decide once a few weeks of the new column's data shows the real fee-vs-headroom tradeoff · you · 2026-08-30
- Score/clock coverage is still watchlist-scoped: `milestone_scan.py` (2026-08-30) builds a broad `event_ticker -> milestone_id` cache, but `live_status._fetch_live_status` still picks WHICH events to poll entirely from its per-tick `markets` argument and only consults that cache for events already picked (and already truncated to `_LIVE_STATUS_MAX_POLL_PER_TICK`), so `game_state` sees exactly the event set it saw before — that branch only removed redundant per-event `get_milestones_for_event` REST calls. Real broadening (polling milestone-known events that never appear in `markets`) is unimplemented and changes what the per-tick poll budget is spent on · design and review it on its own terms, or decide the watchlist scope is what's wanted for score/clock · you (design approval) · 2026-08-30
- PR #307 broadened CLAUDE.md's dimensional-analysis HARD RULE to any arithmetic/unit conversion, not just money/probability math, but `.claude/hooks/guard_workflow.py`'s automated nudge (`post()`, `HOT_PATHS`/`_is_money_ui`) still only fires on the old narrower money/probability-shaped scope — flagged by both the PR's self-review and its independent adversarial review · broaden the hook's detection to match the rule, or decide the wider scope stays session-enforced-only and close · you (design approval) · 2026-08-31
- Exhaustive claudesuperpower.com toolkit scan recommends piloting 4 official `claude-plugins-official` plugins in priority order — `pr-review-toolkit`, `claude-security`, `claude-md-management`, `codspeed` — plus explicit non-adoptions (every Kalshi/Polymarket-named third-party MCP) and one item blocked on the existing "no deployment target" open item (`sentry`/`grafana`). Research → design → implementation-plan pipeline is now complete, each stage through its own self-review + independent adversarial-review + consolidation cycle (`docs/superpowers/research/2026-08-31-claudesuperpower-toolkit-assessment*.md`, `docs/superpowers/specs/2026-08-31-claudesuperpower-plugin-pilot-design*.md`, `docs/superpowers/plans/2026-08-31-claudesuperpower-plugin-pilot*.md`). Two corrections surfaced along the way worth knowing before reading the plan: `claude-security` does NOT grant CLAUDE.md write access (only `claude-md-management` does — scratch-workspace-scoped patches only for `claude-security`), and `codspeed` requires creating/using an external CodSpeed account (`codspeed auth login`, a remote MCP server) — confirmed, not optional, so it's its own separate go-ahead (Task 5) from the other three's plain-install go-ahead (Task 1). Nothing installed, nothing executed — the plan is ready and waiting · give the plan's Task 1 go-ahead (plugins 1–3), and separately decide Task 5 (codspeed's external account), or decline and close · you (design approval) · 2026-08-31
- Weather index ingestion (`docs/superpowers/specs/2026-08-30-weather-index-ingestion-design.md`, PR #263) sat at design-only since 2026-08-30 with no implementation plan ever written. Design-then-plan pipeline now complete, each stage through its own self-review + independent adversarial-review + consolidation cycle (`...-design-review.md`/`...-design-consolidation.md`, `docs/superpowers/plans/2026-08-31-weather-index-ingestion*.md`). The design's own load-bearing caveat (city ranking built on 4-day-stale, Sports-only-scoped catalog data) is now resolved with fresh live data — ranking changed (MIA now ahead of NY), several previously-invisible cities (THOU/TDAL/AUS) came into view — folded into the plan's Task 2. Ingestion only: no settlement-edge validation study, no entry logic, nothing wired into any strategy. Nothing built yet · give the plan's go-ahead to start Task 1, or decline and close · you (design approval) · 2026-08-31
- kalshi-category-data-completeness Task 12 seeded `config/settings.yaml`'s Pyth Commodities feed with 2 real underlyings (gold, silver) rather than `underlying_tickers: ["all"]`, which `docs/kalshi/pyth-value.md:27` documents as a real option to receive every available underlying. The narrower choice was never an explicit go/no-go — it just matched the plan's own example values · confirm the 2-underlying scope is intentional, or decide to widen to `["all"]` (a config-only change, `services/kalshi/websocket.py`'s subscribe/discovery code is already generic to whichever list is configured) · you · 2026-08-31
- `propagate_milestone_winners`'s (`services/market_watch/catalog_scan.py`) `related_market_by_ticker` lookup feeds `ms.get("related_event_tickers")` — documented as EVENT tickers (`docs/kalshi/get-events.md:352-356`) — into `get_markets_by_tickers`, whose own `tickers` filter is documented as MARKET tickers (`docs/kalshi/get-markets.md:227-231`). Found and live-verified during kalshi-category-data-completeness's final whole-branch review (2026-08-31): 710 real related_event_tickers → 0 markets returned via this exact call; a control with real market tickers worked. Pre-existing (`git blame` traces the shape to commit `eeab71a`, before this plan), not introduced by Tasks 1–14, but Task 9's structured-target resolution and Task 14's `candidate_id_mapping` widening both sit on top of it and cannot execute against real winners in production until it's fixed — resolving each `related_event_tickers` entry to its own child markets first (e.g. `get_events(with_nested_markets=True)` or a per-event `get_markets(event_ticker=...)` call) is a separate, non-trivial change to this function's data-fetching shape · decide priority and approach, or accept this milestone-winner-propagation path stays non-functional for structured/political-race markets for now · you (design approval) · 2026-08-31
- `scripts/cleanup-worktrees.sh` reproduced 2026-08-31: its own `is_ancestor` check correctly compares a branch against freshly-fetched `origin/main`, but the actual deletion (`git branch -d "$branch"`) doesn't pass that context to git — when the branch's remote-tracking ref is already gone (the normal post-merge state) and the primary checkout isn't on `main` itself (it was on `feat/realtime-data-plane-remediation`), `git branch -d` falls back to checking merge-into-HEAD instead, spuriously refuses, and aborts the whole run under `set -euo pipefail` — leaving a worktree already `git worktree remove`d with its branch still dangling (hit live on `feat/kalshi-category-data-completeness`, worked around by hand this session with `git branch -D` after independently re-confirming the origin/main-ancestor check). Fix is a one-line change (`-d` → `-D`, since the script's own prior `is_ancestor` check against `origin/main` already establishes safety independent of what the primary's current branch is) plus a `tests/test_cleanup_worktrees.py` case for "primary not on main + branch merged into origin/main but not local HEAD" · fix it (small, needs its own review cycle per the diff-size-independent HARD RULE) or decide to keep working around it by hand · you · 2026-08-31
- 2026-08-31 line 20 item (3)'s "these will age out on their own" hope did NOT pan out: `python -m tools.soak_analyzer` re-run 2026-09-01 (~14h past the named ~08-31 16:11 UTC boundary) still VERDICT FAIL — `capture_writer_health` (87 faults) and `exit_engine_faults` (314) are not stale pre-restart tail, verified live via `/api/health/faults`: `capture_writer`'s `flush_retained_on_lock`/"database is locked" fault (issue #211, already tracked) last occurred 2026-09-01T06:06:23Z, and `exit_engine`'s `stale_price_uncorroborated` fault last occurred 2026-09-01T06:07:09Z — both minutes before this check, i.e. actively recurring, not decaying. `two_consumer_mode` permanence stays open, do not close the gate on the next clean-looking read without checking fault recency the same way. `exit_engine`'s `stale_price_uncorroborated` pattern has no tracked issue yet (unlike capture_writer's #211) · root-cause both via systematic-debugging (capture_writer's is likely the same lock-contention path as #211; exit_engine's needs its own trace) before any further two_consumer_mode boundary check is treated as informative · you · 2026-09-01
- whale-confidence-scoring-remediation Tasks 1-9 (PR #388) fix `_bucket_win_rates`'s tie-blind bucketing, so on real production data 5 of 9 factors now correctly read `data_status: "contaminated"` (previously incorrectly measured as clean) — measured live before merge, independently reproduced twice (final-branch review + PR adversarial review, both against the real 103k+-row `signal_log.db`): `suggested_weights` collapses from 7 surviving factors (`context 0.34, cluster 0.19, trend 0.18, agreement 0.16, depth/unusualness/proximity 0.04` each) to just 2 (`context_factor 0.95, depth_factor 0.05`), and `blended_weights_for_auto_apply`'s `context_factor` would move `0.3434 → 0.5437` if applied — the fix makes correctly-disqualified factors MORE dangerous if clicked, not equally dangerous, since `_suggested_weights` has no `data_status` awareness and no minimum-survivor guard once factors are (correctly) dropped. `confidence_calibration.auto_apply_enabled` stays `false` (verified, untouched by this plan) so nothing writes this automatically, but `POST /api/confidence-calibration/apply` (the dashboard's manual "Apply suggested weights" button) has no guard against writing this concentrated blend — Task 12's `measurement_is_valid` gate is the design's own named fix for exactly this, correctly blocked behind Task 10's hard soak-time re-measurement gate. Not a new risk (the pre-fix suggestion was already contaminated-derived, just silently so) but the window is real and unguarded in code between this merge and Task 12 landing · do not click "Apply suggested weights" on the whale-confidence-calibration dashboard panel until Task 12 ships (Tasks 10-16 need a real soak period first, not startable this session) · you (avoid clicking) / me (land Task 12 once Task 10 clears) · 2026-09-01
- whale-confidence-scoring-remediation's Task 1 tie-detection (`_tied_run_size`) is a real improvement (PR #388's independent adversarial review confirmed 4 of 5 real-data contamination flags are unambiguously correct) but has 2 known precision gaps, found by that same adversarial review with concrete falsifying data, not fixed in the PR (algorithmic refinement, not a "make the docstring honest" fix): (1) it flags contamination on the FULL tied-run length at a cut, not the minority side actually crossing the boundary — real example: `unusualness_factor`'s cut has a 1,662-row tied run but only 381 rows (1.1% of a 34,426-row bucket) are on the minority side, yet the whole run trips the floor, discarding a real −16.1pt measurement over ~1% boundary arbitrariness; (2) `max(30, 0.005*n)` mixes an absolute floor with a fractional one, creating a blind band below n≈6000 where identical-valued rows split by arrival order can read `"ok"` when they shouldn't (constructed counterexample: n=60, a tied block of 30 at a cut reads `"ok"`, one more row flips it to `"contaminated"`) — not reachable at today's real n=103k+ (relative term dominates), but latent at `min_resolved_signals: 50` · consider before Task 10's re-measurement gate treats today's algorithm as final, or before `min_resolved_signals` is ever lowered · you (design call — a real algorithm change, not a doc fix) · 2026-09-01
- whale-confidence-scoring-remediation's Task 9 diagnostic (`check_confidence_input_coverage`, `GET /api/quality/summary`) had its expensive COMPUTE half fixed in-PR (~20x reduction, PR #388) but its I/O half is untouched and real: `resolved_signals_with_factors()` (no `since_ts` passed from this route) does a full unscoped table fetch, independently measured at ~1.0-1.1s against the real 103k+-row DB, on a route polled every ~5s while the dashboard's Terminal tab is open, on the same 2-worker `tick_executor` pool the trading loop shares — roughly a 20% duty cycle on that pool for this one diagnostic alone. Confirmed by 2 independent reviews (final-branch review's own out-of-scope note + PR's adversarial review) · bound the fetch (e.g. a `since_ts` default, or a cheaper COUNT-based coverage query) or decide the duty cycle is acceptable and close · you (design call) · 2026-09-01
- `yes_ask_dollars`'s value when a market genuinely has no resting ask (as opposed to the field being absent from the payload, which Task 4 of whale-confidence-scoring-remediation now handles as `None`) is unconfirmed against `docs/kalshi/` — `get-market-orderbook.md` doesn't state a no-ask sentinel, but the mirrored `docs/kalshi/sample_event_response.json:158` shows `"1.0000"` for what may be exactly that case. If `"1.0000"` is Kalshi's own no-ask sentinel rather than a real price, `raw_spread`/`input_coverage`'s absence-rate would understate how often spread is genuinely unmeasurable · verify against a live no-ask market or current official Kalshi docs before Task 10/16 reads spread coverage numbers as ground truth · you or me (needs live-API or docs verification, not resolvable from the current local mirror) · 2026-09-01
