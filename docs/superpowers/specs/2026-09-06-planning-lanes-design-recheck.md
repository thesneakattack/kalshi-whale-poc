# Fix-list recheck: planning-lanes design (round-2 revision)

Date: 2026-09-06 (evening). Scoped recheck of
`2026-09-06-planning-lanes-design.md` ("fix-list recheck applied") against
the 10 required + 2 optional fixes in
`2026-09-06-planning-lanes-design-adversarial-review-round2.md` §6.
Independent Agent call, no memory of either authoring conversation. Not a
full adversarial review. The review's binding condition was honoured:
**every changed lane-table row below was re-derived by me from the file's
own module docstring (`ast.get_docstring`) or package listing, not
accepted from the revision's text.** All `gh`/`git` figures re-run live.

## 1. Per-fix verdicts

| Fix | Verdict | Evidence |
|---|---|---|
| **1 Table under one rule** | **PARTIALLY APPLIED** | All 11 disputed file rows re-derive to the assigned lane from their own docstrings (table below); `market_lookup.py` is listed in exactly one lane (L5); no `.py` is a member of two lanes (7 names recur in the table, every second occurrence is a cross-reference or precedent mention, none a listing); all 66 `services/` units are covered (`index_feed/` 3 files, `whale_stream/` 3 files, `reset/` all accounted). **Lane 7's listing is NOT APPLIED — it is now factually inverted:** `ls services/config/` = `README.md __init__.py config_bounds.py config_overrides.py config_paths.py config_performance.py config_store.py routes.py`; `ls services/config_*.py` = no matches. The row asserts `routes.py`/`config_paths.py` are "the only two files actually inside this subdirectory" and that `config_store/bounds/overrides.py` are "flat top-level modules not yet moved into it". `services/config/__init__.py`'s docstring says the opposite verbatim ("config_store.py, config_bounds.py, config_overrides.py, and config_performance.py moved in here 2026-08-27"). Probable source of the error: `services/config/README.md` still says they "stay flat for now" — a stale README, i.e. the design's own clause-(a) case, mis-applied on its own turf. |
| **2 Rule clauses a/b/c** | **APPLIED** | §2 has all three, each tied to a named case: (a) `market_analyst_agent/` — `kalshi_trade_tape.py:212` really is `market_analyst_agent.analyst_lean(ticker, ...)`, contradicting the package docstring's "never called from anywhere hot" implication; code wins on the fact, docstring fixed in the same PR, lane unchanged (L4) — matches what the reviewer asked; (b) `market_lookup.py`; (c) `config_performance.py`. Two notes: §2(b) attributes the phrase "already-in-memory `services.app_state.state`" to `state_view.py` — it is `market_lookup.py`'s own sentence; `state_view.py`'s real parallel is "shared home rather than being owned by any one of those" (the Lane 5 row attributes it correctly). And two rows (`market_history`, `series_watcher`) rest on a docstring-*ordering* tiebreak that is not one of clauses a/b/c — see §2 below. |
| **3 Multi-lane rule** | **APPLIED** (one factual correction needed) | §5 replaces the reference count with the initiative's own Goal/spec subject; "nearly 3x" survives only as the claim being retracted; small-PR case stated on #636. `gh pr view 636 --json files,title`: title `fix: gate_summary() SQL GROUP BY + async sibling (partial #605 fix)`, **5 files** — `docs/event-loop-blocking-routes-census-2026-09-03.md`, `services/analytics/routes.py`, `services/candidate_log.py`, `tests/test_analytics_routes.py`, `tests/test_candidate_log.py`. The design says three. The two `tests/` files are Lane 9 by the design's own table, so "its files are now both Lane 4 plus one docs file" is an incomplete census. The title-based rule still yields `lane:4`; the sentence's justification is what's wrong, not the label. |
| **4 Cross-lane form + `concern:*`** | **APPLIED** | `CONCERN_HOTPATH = "concern:hotpath"` + `CONCERNS` dict named for `labels.py`, applier stated (data-plane HARD-RULE defect class), worked examples #632/#636/#637. `depends-on:#N` is real: `gh issue view 77 --json labels` → `depends-on:#75`, `depends-on:#76` (labels, not body text — #77's body has no "depends"); created/reconciled by `tools/kanban_sync/sync.py:382-396`, `plan_tasks.py:127`, `github_client.py:225`. `gh label list`: all 8 `area:*` definitions exist (`architecture frontend kalshi-integration misc production-readiness realtime strategy workflow-tooling`; 9 open issues carry one: realtime 7, kalshi-integration 1, workflow-tooling 1) and are slated for deletion in step 2; `phase:implementation-plan` confirmed on #75, #76, #77 and slated. Two notes: it is a *label* family, not a "marker" (in `kanban_sync` "marker" means the `<!-- autotrade-sync: kind:key -->` comment); and per the installed plugin's `references/dependency-chain.md:8-13`, `depends-on:#N` makes the labelled issue **unclaimable until #N is `status:done`** — it encodes work ordering, not a static "this file calls into that lane" reference. The design should say the link is used only when the dependent-lane issue genuinely waits on the owner-lane issue; otherwise a body mention. Not a fix failure (the reviewer offered this option). |
| **5 Touchpoints** | **APPLIED** ("complete list" slightly over-claimed) | Every cited line verified against the file: `__main__.py:26` import of `parse_track_items`; `:37` `ACTIVE_TRACKS_BOARD_PATH = Path(...)`; `:39` `KNOWN_SOURCES` has `"track"`; `:134` `parse_track_items(ACTIVE_TRACKS_BOARD_PATH.read_text())`; `:224` `list_plan_candidates(PLANS_DIR, ACTIVE_TRACKS_BOARD_PATH.read_text())`; `:275` help `worktree,roadmap,track,plan`; `:231-236` track-marker comment; `:302-303` `--parent-issue` help. `sources_plan.py:27` `list_plan_candidates(plans_dir: Path, active_tracks_board_text: str)`, glob at `:29`; run live: **59 candidates, 24 companions + `README.md` included, 3 board-referenced plans** (realtime remediation, economic-strategy-effectiveness-investigation, economic-strategy-remediation) — all as the design states. `labels.py:68` `SYNC_MARKER_KIND_TRACK = "track"`. `.claude/skills/checkpoint/SKILL.md:69` = `kanban_sync sync --sources worktree,roadmap,track`, `:70` = `quality_coordination`. `.claude/skills/kanban-board-sync/SKILL.md:31` = the `--dry-run` sync line; step 3 at `:35-36`. `tools/quality_coordination.py:71` = the "paused, not stalled" active-tracks-board.md note. `ROADMAP.md:66` = board link. Execution-program doc `:994-996` = "Track C table, which is the authoritative live status tracker … rather than this section's own stale requirements below". Tests: `test_kanban_sync_sources_tracks.py` exists, 15 `def test_`; `test_kanban_sync_main.py:391,399,406,412` use `"track"`; `test_kanban_sync_sources_plan.py:6,20` write the board file; `kind="track"` fixtures in `test_kanban_sync_sync.py:70,363-388,428`, `_markers.py:13-14`, `_models.py:7,18`. Cosmetic: `__init__.py:2-3`, `sync.py:279-282` confirmed. Orphan companion: its own header says `Plan: …/2026-09-03-persistence-layer-db-migration-implementation.md lines 1597-1715 (Task 8)`; that plan's `### Task 8` (`:1627`) is `services/candidate_ledger.py migrates to services/db.py`, the parent file exists — and the *other* persistence-layer plan's Task 8 is aiosqlite sequencing, so the design picked the right one. Notes: `:37` is the Path constant, not a `.read_text()` call site — the design's parenthetical says ":37 and :134 (… two separate call sites)"; the two read sites are `:134` and `:224`. The "complete list" misses `tests/test_kanban_sync_labels.py:28` (asserts `SYNC_MARKER_KIND_TRACK`; breaks when the constant goes) and `kanban-board-sync/SKILL.md:3` and `:36` (both name the board; round 2 listed them per the reviewer, the revision dropped them). |
| **6 §5 exemption** | **APPLIED** | `grep -in candlestick docs/open-decisions.md` → line 21, present and **committed at HEAD** (`69925c2 2026-09-06 docs: record feat/candlestick-volatility as parked-by-decision in open-decisions`; `git diff HEAD` empty). The line cites `docs/branch-audit-2026-09-05.md §2`, notes the #398 comment is about a different branch, and states the AQC stale signal stays. `git ls-remote origin` → `ad24098 refs/heads/feat/candlestick-volatility`; worktree `.claude/worktrees/candlestick-volatility` at `ad24098`. §7 of the design matches. |
| **7 Numbers** | **APPLIED** | Live 2026-09-06 PM: open issues **146**; `"Plan:" in:title` returns 8 rows of which **6** are real `Plan:` titles (#89 #331 #321 #81 #448 #320; #462 and #615 are search noise); `type:plan-task` **76**; `phase:plan` 9; `git grep -n 'docs/superpowers/plans/' -- ':!docs/superpowers/plans/'` = **260** lines (172 files; `-o` occurrence count also 260); same over `services tools tests .claude/hooks main.py` = **118**; `.github/workflows` 3; board-referenced plans 3. All five design figures hold. Cosmetic: "155 → 146, 14 closed" does not reconcile arithmetically (155 − 14 = 141); the reviewer's own §2.G table has the same gap, carried forward; the 146 is what is verified. |
| **8 Refs** | **APPLIED** | `kanban-board-sync/SKILL.md:69` survives only in the sentence naming it wrong (design `:218`); `checkpoint/SKILL.md:69-70` used in §6 and §8; `:31` added; "round 1 named none of these" disambiguated to the round-1 *design*. Cosmetic: §8's parenthetical says the mislabel was "in this section", but the tooling paragraph now lives in §6; and "not the Track C fix" uses round-1 consolidation numbering (#6 = Track C) while the header says bracketed numbers are round-2 numbers (#6 = §5 exemption). |
| **9 Step-1 tables reviewed** | **APPLIED** | §6 step 1: tables "get their own self-review + independent adversarial review + consolidation before step 2 starts", with the 1-omission/7-inconsistency rate cited as the reason. |
| **10 `plans/README.md`** | **APPLIED** | §6 step 5: "retire, not regenerate", with the reason (no generator exists) and the replacement (`lane:*` + the checked-in table) stated. |
| Optional 11 | noted | §11/§12 name the `#326` state-reason correction; `gh issue view 326` still `CLOSED / COMPLETED` — non-blocking either way. |
| Optional 12 | noted, overstated | §3 L6 row + §12 note `services/reset/`. Accuracy: it has no `__init__` docstring and no README (confirmed), but `routes.py` ("Danger Zone routes…"), `reset_log.py` ("Audit trail for every Danger Zone reset…") and `trade_archive.py` ("Permanent, append-only archive of paper-trading history…") all carry module docstrings — clause (c) reads them and gives L6 directly. "The one unit the rule literally cannot read" is not true; non-blocking. |

### Fix 1 — per-file re-derivation (my reading of each docstring against §2's rule)

| File | Design lane | Docstring sentence I rest on | Clause | Follows? |
|---|---|---|---|---|
| `candidate_log.py` | L4 | "This does NOT act on rejected candidates (no trade is ever placed from this module) - it only observes"; exists because advisory_engine's recommendations "return None … this module is what that data would come from" | base | **Yes** — identical shape to `settlement_edge.py` ("This module never trades. It records and scores.") → same lane. The writer claim (`capture_writer.py`, "rejection_events writes route through services/capture_writer.py") is a declared L5 dependent, correctly noted. |
| `series_watcher.py` | L1 | "captures the raw exchange data this app otherwise throws away, then reconciles 'how often were the whales right' against 'how often did I actually win.'" | base (+ uncodified ordering tiebreak) | **Defensible, not uniquely derived.** Single file, two declared halves, no internal boundary — §2 as written gives no answer. The row invokes an "ordering tiebreak" that §2 does not contain, and the docstring's own next line, "Two halves, and the first exists because of the second", argues the *second* half is the raison d'être. L1 is the reviewer's own reading and I would not fail the row on it, but the tiebreak must be a stated clause or the row rests on nothing re-appliable. |
| `market_history.py` | L1 | "Real Kalshi market data, logged over time"; job 1 "rolling per-market snapshot log", job 2 "Settlement outcomes (real, from Kalshi's market.result field) plus compute_hypothetical_trades()" | base, `game_state` precedent ("Durable time series of real in-game state … captured alongside the market data this app already stores" → L1) | **Yes.** Note: the row cites the precedent, not an ordering tiebreak — yet the `series_watcher` row says "same ordering tiebreak as `market_history.py`". The two rows do not describe the same rule. |
| `market_lookup.py` | L5 | "extracted into its own module … rather than owned by any one of those, since all three already depended on it" | (b) | **Yes.** `state_view.py`: "get their own shared home rather than being owned by any one of those" — same disclaimer shape. Listed once. |
| `whale_stream/index_stream_handlers.py` | L1 | "The index-stream callback layer - CF Benchmarks/Pyth index ticks, feeding settlement-edge observation capture." | base | **Yes.** "Transport-level only" is gone. Lines 242-249 call `settlement_edge.record_observation` (L4), `settlement_edge_entry.evaluate_entry` (L3) and `decision_bridge.handle_settlement_edge_entry` — the last is **L2** by the table, so the outward dependents are L2/L3/L4, not only L3/L4 as the row says. Minor. |
| `config/config_performance.py` | L4 | "so services/advisory/advisory_engine.py can score each config variant on its own resolved trades" | (c) | **Yes** for the lane. The file's location is misdescribed everywhere the design mentions it (see Fix 1 / Lane 7). |
| `ws_manager.py` | L8 | "The dashboard's own push-update websocket connection registry (GET /api/ws in main.py)" | base | **Yes**, and now consistent with `history_push.py`. |
| `history_push.py` | L8 | "The History tab's event-driven push mechanism"; "Modeled on services/ws_manager.py's own extraction shape" | base | **Yes.** |
| `latency_agg.py` | L6 | "Bounded, allocation-free latency accumulators for hot-path telemetry … Design constraints (services/observability/README.md 'Hot-path impact')" | base | **Yes.** Removed from L5. |
| `whale_pipeline_perf.py` | L6 | "Stage-by-stage timing and counters for the whale-trade pipeline … Persistence and window ownership are services/observability's" | base | **Yes.** Removed from L2. |
| `data_quarantine.py` | L4 | "Non-destructive protection of the analytics dataset from deliberate test windows." | base | **Yes** (L6 also admissible per the reviewer; L4 chosen with a stated reason). Removed from L5. |
| Lane 7 listing | — | `services/config/__init__.py`: "config_store.py, config_bounds.py, config_overrides.py, and config_performance.py moved in here 2026-08-27" | — | **No.** Row asserts the inverse of `ls`. |

## 2. Not applied, or newly broken by the revision

**Blocking (fix-list failure or unresolved reference):**

1. **Fix 1 / Lane 7 listing — NOT APPLIED, inverted.** Must read: `services/config/` = `routes.py`, `config_paths.py`, `config_store.py`, `config_bounds.py`, `config_overrides.py` (+ `config_performance.py` → L4 by clause c); there are no flat `services/config_*.py`. Cite `services/config/__init__.py`'s docstring; note `services/config/README.md` is stale on this point.
2. **§6 step 3 cites "§3 names this doc as the sole surviving owner of production-sequencing gates" — §3 does not, and nothing in the revision does.** `grep` for `standing`, `Program 3R`, `gate table`, `#576`, `#624`, `#625`, `#627`, `#630` over the design: zero hits (`Track C` and `execution-program` each appear once, only inside that step-3 bullet and §8's parenthetical). The round-2 content the reviewer confirmed correct in §1 — "#6 Track C" (gates owned by the execution-program doc), "#6 standing decisions" (port the 5 unchecked board items at `:220-237` to `docs/open-decisions.md`, checking duplicates), the 7-PR mapping under the table (#624/#625/#627/#630 now absent), and #576's L5→L1 correction — was silently dropped. Consequences: the `§3` reference does not resolve, and step 4 (board → `docs/archive/`) now has no stated destination for the Track C gate table or the 5 standing decisions. Restore the disposition (or point at where it lives) so the reference resolves.
3. **§3 "`CONCERNS` dict mirroring the new `LANES` map (§6)"** — `LANES` is defined in §8 rule 5; §6 is Migration. Renumbering artifact; the reference does not resolve.

**Non-blocking corrections (accuracy, not scope):**

4. `series_watcher.py` / `market_history.py` rows rely on a docstring-ordering tiebreak §2 never states; the two rows also don't describe the same rule (one cites a precedent, the other "the same ordering tiebreak"). Add it as a clause or restate the rows.
5. §2(b) misattributes market_lookup's "already-in-memory `services.app_state.state`" sentence to `state_view.py`.
6. PR #636 is 5 files, not 3; two are `tests/` (Lane 9 by the table). The `lane:4` label still follows from the title; fix the census sentence.
7. `__main__.py:37` is the Path constant, not a `.read_text()` site; the two read sites are `:134` and `:224`.
8. Touchpoint list misses `tests/test_kanban_sync_labels.py:28` and `kanban-board-sync/SKILL.md:3`, `:36`.
9. `index_stream_handlers.py` row: `decision_bridge` (L2) is also an outward dependent.
10. `depends-on:#N` gates claimability (plugin `dependency-chain.md:8-13`); say it is used only where the dependent issue truly waits on the owner-lane issue. Also it is a label family, not a "marker".
11. §8 parenthetical: "in this section" (it's §6 now) and "Track C fix" (round-1 numbering vs the header's round-2 numbering).
12. Optional #12: `services/reset/`'s three modules do have docstrings; "cannot read" overstates.
13. "155 → 146 via 14 closed" does not reconcile (carried from the reviewer's table).

## 3. Lane-list check (the review's flip-to-full-cycle condition)

The table has the same 9 lanes with the same names the reviewer references (1 Kalshi & index data ingestion · 2 Whale signal detection & calibration · 3 Strategy, risk & execution · 4 Analytics, advisory & research · 5 Runtime infrastructure · 6 Observability, quality & safety infra · 7 Config & control plane · 8 Frontend & dashboard · 9 Tooling, CI & process governance). Every fix is a row reassignment: `market_lookup.py` went into the existing L5 (no "shared runtime helpers" lane was created); telemetry moved to L6 by *rows*, not file moves; Lane 5 went 16 → 14 members (−`ws_manager`, −`latency_agg`, −`data_quarantine`, +`market_lookup`), which is consistent with the reviewer's count. **No lane was added, merged, or removed. The design's claim on this point holds; the reviewer's NO-GO trigger is not met.** Items 1-3 above are assignment-level and reference-level, not scope changes.

## 4. Verdict

**RECHECK FAILED.** Nine of ten required fixes are applied and verified; the lane list is unchanged. Outstanding before this can sign off:

1. **Fix 1, Lane 7 sub-item** — the listing is factually inverted against `ls services/config/` and `services/config/__init__.py`'s own docstring (item 1 above).
2. **§6 step 3's `§3` reference does not resolve** and the Track C / standing-decisions disposition it depends on was dropped from the revision (item 2).
3. **§3's `(§6)` → `(§8)`** for the `LANES` constant (item 3).

Fixing all three is text-only and stays within the same 9 lanes, so per the review's own procedure it is a re-check of these three items, not a round 3 — unless restoring item 2 changes what the migration archives, in which case that is new content the two prior reviews never saw. Items 4-13 are accuracy corrections the author should fold in at the same time; none of them changes a lane.
