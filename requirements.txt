# 2026-08-23 pip-audit fix: 0.115.0 pulled starlette 0.38.6, which carried 7
# known CVEs (PYSEC-2026-161/248/249/1941/1943/2280/2281) - three of them
# (161/248/249) only fixed starting at starlette's 1.x line, not backported
# to 0.x. 0.134.0 is the lowest fastapi release that pulls starlette>=1.x
# (verified via `pip install --dry-run --report` across the intervening
# versions - 0.132.0 still resolves starlette 0.52.1); it resolves to
# starlette 1.6.0, past every fix version. Also forces a pydantic bump (see
# below) - unrelated to the starlette CVEs, just fastapi's own floor since
# ~0.116.
fastapi==0.134.0
uvicorn[standard]==0.32.0
httpx==0.27.2
pyyaml==6.0.2
# services/config_store.py's own load/write engine (2026-08-23) - PyYAML's
# safe_load/safe_dump pair can't preserve comments through a round trip,
# which was silently stripping every hand-written comment out of
# config/settings.yaml on each live edit (see that module's own docstring).
# ruamel.yaml's round-trip mode fixes this. pyyaml above stays - still used
# directly by tests/test_config_store.py's own fixture setup, and safe to
# keep as a general-purpose YAML dependency independent of this change.
# Verified against the real settings.yaml before pinning, not assumed
# compatible: exact indent/formatting match, CommentedMap's dict-ness
# (isinstance/equality/JSON serialization), and identical exception
# behavior to PyYAML across 179 sampled truncation points of the real file.
ruamel.yaml==0.18.17
# Pulled up from 2.9.2 by fastapi==0.134.0's own floor (not a CVE fix on
# pydantic's own account - it wasn't in the pip-audit findings). Verified
# via the same dry-run resolution as fastapi above.
pydantic==2.13.4
# 2026-08-23 pip-audit fix: 1.0.1 had PYSEC-2026-2270 (symlink-following
# arbitrary-file-write in set_key/unset_key). Fixed in 1.2.2.
python-dotenv==1.2.2
# 2026-08-23 pip-audit fix: 43.0.3 had 6 known CVEs, highest fix version
# 49.0.0 (PYSEC-2026-3553/3554). Went one further to 50.0.0 after finding
# PYSEC-2026-3552 (a PKCS7/S-MIME Bleichenbacher timing oracle, introduced
# in 44.0.0 - not applicable to this app's actual usage, which is RSA-PSS
# request signing in services/kalshi_account_client.py, not PKCS7/S-MIME -
# but fixed for free by going to the version pip-audit reports clean
# against, rather than landing one CVE short of a full fix).
cryptography==50.0.0
itsdangerous==2.2.0
# Official Kalshi SDK. Requires Python >=3.13 (see Dockerfile) - every
# version after 3.2.0 does; only 3.2.0 supports older Python, and 3.2.0 is
# stale relative to Kalshi's current API (verified 2026-08-07: its Pydantic
# models reject real positions/fills responses entirely). Pin exact version
# rather than trusting `pip index versions`, which silently resolved to the
# stale 3.2.0 under Python 3.11 with no warning that newer releases existed.
kalshi-python-async==3.27.0
# Not declared as a kalshi-python-async dependency despite being imported
# directly by its configuration.py - a real packaging gap on Kalshi's side.
urllib3==2.7.0
websockets==17.0.1
# aiosqlite - services/diagnostics/diagnostics.py + services/series_watcher.py's
# read-only functions run through this now (event-loop-blocking-elimination
# Fix 2, 2026-09-01) instead of raw sqlite3 + a hand-rolled ThreadPoolExecutor,
# so the asyncio event loop is never blocked by their disk I/O. 0.22.1 is the
# current latest stable (confirmed via `pip index versions aiosqlite` inside
# the fastapi container, not assumed) - not previously a dependency of this
# repo (confirmed: absent from this file and from a direct `import aiosqlite`
# probe inside the running container, which raised ModuleNotFoundError).
aiosqlite==0.22.1
# services/market_analyst_agent.py - optional, only imported/used when
# ANTHROPIC_API_KEY is set and market_analyst.enabled is true (see that
# module's docstring). Version confirmed directly via `pip index versions`
# at the time this was added, not assumed - see requirements.txt's own
# kalshi-python-async comment above for why that distinction matters here.
anthropic==0.121.0
