"""Config module - see config_paths.py and routes.py. Started 2026-08-21 as
part of main.py's modularization pass (folder-per-concern, direct
instruction). services/config_store.py, config_bounds.py, config_overrides.py
and config_performance.py are already clean, self-contained modules from
before this pass and stay where they are for now - see the modularization
plan's "Folder-per-concern restructuring" section for why they aren't
retroactively moved into this package yet."""
