"""
Advisory - the rule-based config-tuning recommendation engine (see
docs/advisory-engine-plan.md). Reads trade/config-performance history and
emits concrete, explainable suggested config values a human (or, opt-in,
an auto-apply path) can act on. See advisory_engine.py and README.md.
"""
