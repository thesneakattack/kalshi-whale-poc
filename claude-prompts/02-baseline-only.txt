Execute Task 0 only from:
docs/superpowers/plans/2026-08-24-quality-control-plane.md

Do not begin Task 1.

Before running anything destructive or test-related, confirm:
- tests must never touch live data/*.db;
- real trading remains disabled;
- no historical DB is reset or modified for verification.

Run the baseline exactly against current HEAD:
- pytest collection
- full backend pytest suite
- frontend npm ci
- frontend lint
- frontend build
- verify whether rebuilding changes static/js/dashboard.bundle.js
- inspect current CI workflow definitions
- git status after all baseline commands

Report:
1. test count,
2. test result,
3. frontend lint result,
4. frontend build result,
5. whether generated frontend output was already synchronized,
6. any pre-existing failure,
7. any plan assumption disproved by baseline,
8. whether it is safe to begin the first implementation task.

Stop after the baseline report.