Do not declare the Quality Control Plane complete yet.

Execute the plan's final verification in full.

In addition to normal success-path tests, deliberately prove the new guardrails using isolated fixtures/temporary copies, never real data:

- an unmounted router is detected,
- an unwired scheduler/background service is detected,
- test access to repo data/*.db is blocked before SQLite creates/opens it,
- a statically resolvable frontend call to a missing backend route is detected,
- changed Kalshi-doc fixture content triggers drift,
- stale project manifest is detected,
- browser E2E catches a fatal/severe frontend error,
- observability survives restart and respects retention,
- storage-health checks never repair/mutate databases,
- /api/quality/summary performs no live Kalshi call,
- research snapshots do not apply config or trading changes.

Then run:
- full pytest suite,
- frontend npm ci/lint/build,
- generated bundle sync check,
- static quality audit,
- browser E2E,
- project manifest check,
- git diff --check,
- git status --short.

Review every entry in tools/quality_audit/baseline.json individually:
- intentional exception,
- real debt with ROADMAP item,
- or resolved/removable.

Inspect staged/untracked files for accidental:
- data/*.db,
- .env,
- credentials,
- auth secrets,
- browser profiles,
- temp logs,
- unrelated build output.

Finally perform a code-review pass focused on:
1. any pytest path that can still touch live data,
2. runtime DB work blocking hot paths,
3. quality endpoints making unexpected network calls,
4. unwired routes/background services,
5. false-positive CI rules,
6. unbounded metrics/storage,
7. restart/reload behavior,
8. SQLite concurrency,
9. browser E2E depending on real environment state,
10. Kalshi assumptions not grounded in docs,
11. research/advisory accidentally mutating config,
12. new services missing backup/retention/diagnostics/CHEATSHEET coverage,
13. CI steps that silently skip instead of verify,
14. ROADMAP/status/CLAUDE drift,
15. the quality-control system creating new reliability problems.

Fix confirmed issues in focused commits, rerun affected verification, then report completion evidence and final HEAD.