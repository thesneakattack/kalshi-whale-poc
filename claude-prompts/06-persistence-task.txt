This task adds or changes a stateful/SQLite-backed service.

Before broadly importing or wiring the service:
1. identify every DB_PATH/persistent file it owns,
2. add it to centralized pytest runtime isolation,
3. add/extend the hard guard proving tests cannot reach repo data/,
4. ensure the static quality audit recognizes the new persistence owner,
5. use additive schema creation/migration only,
6. define retention/backups explicitly,
7. confirm no test opens live historical DBs.

The persistence safety guard must land with the feature, not afterward.

No verification step may delete, reset, vacuum, rewrite, or inject synthetic rows into real data/*.db.

Proceed with the current numbered task only after these conditions are accounted for.