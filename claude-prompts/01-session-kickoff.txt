Read CLAUDE.md in full before doing anything else.

Then read CLAUDE_EXECUTION_PROMPT.md in full and follow it.

Then read:
- docs/superpowers/specs/2026-08-24-quality-control-plane-design.md
- docs/superpowers/plans/2026-08-24-quality-control-plane.md

Do not modify code yet.

The current repository and git history are authoritative over anything stale in the plan. The design spec defines the architectural intent. The implementation plan defines the intended execution sequence. Reconcile both against current HEAD before acting.

Establish the current repository state:

git status --short
git branch --show-current
git rev-parse HEAD
git log -20 --oneline

Also inspect:
- ROADMAP.md
- static/status.html
- relevant service CHEATSHEET.md files
- .github/workflows/
- frontend/package.json
- tests/conftest.py

Then compare current HEAD against the Quality Control Plane plan.

Report back with:
1. Current HEAD and branch.
2. Whether the working tree is clean.
3. Current test-suite size if readily measurable without modifying anything.
4. Current frontend build/lint setup.
5. Which plan tasks are completely unimplemented.
6. Which are partially implemented.
7. Which appear already implemented or superseded since the plan was written.
8. Any conflict between CLAUDE.md/current code and the plan.
9. The exact first task that should be executed.
10. Any pre-existing issue that must be resolved before Task 0.

Do not make changes yet.