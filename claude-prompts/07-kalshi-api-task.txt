This task touches Kalshi-shaped API or WebSocket behavior.

Before changing code or fixtures:
1. identify the exact Kalshi endpoint/channel/message/order/lifecycle behavior involved,
2. read the corresponding current file(s) under docs/kalshi/,
3. compare documented fields/semantics against current implementation,
4. if this task is specifically about documentation drift and the local mirror may be stale, verify against the current official Kalshi docs,
5. derive contract fixtures/tests from the documented contract, not memory and not merely current parser behavior.

Pay special attention to historical bug classes:
- deprecated/renamed side fields,
- singular/plural message types,
- fill identity fields,
- determined vs finalized lifecycle semantics,
- current order-create/cancel schema.

Do not use real-trading credentials or place/cancel real orders in tests or canaries.

Then continue the current numbered task.