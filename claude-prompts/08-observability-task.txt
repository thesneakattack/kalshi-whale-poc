This task touches observability, metrics, performance, latency, CPU, REST usage, stream handling, or database efficiency.

Before adding instrumentation or changing performance-sensitive code:
1. inventory existing measurements first:
   - last_tick_duration_sec,
   - tick_phase_timings,
   - last_tick_rate_limit_hits,
   - trade_stream_perf,
   - stream message/drop counters,
   - task supervisor state,
   - fault log,
   - alerting state,
   - pipeline last-write ages,
   - backup state,
   - existing HTTP/rate-limit counters.
2. measure current behavior before optimizing,
3. persist aggregates/snapshots rather than every event/message/request,
4. keep cardinality bounded,
5. avoid Python stack inspection in the hot path,
6. verify new observability work does not measurably slow websocket handlers or the trading loop,
7. background non-trivial work through existing task-supervisor patterns.

If measurements contradict the initial hypothesis, follow the measurements.

Proceed with the current numbered task only.