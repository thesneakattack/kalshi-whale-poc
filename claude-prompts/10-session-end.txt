Stop implementation after the current numbered task is fully verified and committed.

Prepare a clean session handoff.

Report:
1. current branch,
2. current HEAD SHA,
3. tasks completed this session,
4. last fully completed task,
5. first incomplete task,
6. any partially completed work — ideally none,
7. full-suite status,
8. frontend lint/build status,
9. static quality-audit status,
10. browser/API integration status if available,
11. current CI status if available,
12. newly discovered ROADMAP items,
13. accepted quality-audit baseline findings,
14. runtime measurements that should be rechecked next session,
15. whether ROADMAP.md, static/status.html, and relevant CHEATSHEETs are synchronized,
16. uncommitted files and why.

Run:
git status --short
git log -8 --oneline

Do not start another task just because context remains.
Do not describe the session as clean if verification is outstanding.